@page
@model CampusConnect.Pages.Manager.ReviewRequestsModel

@{
    ViewData["Title"] = "Review Requests";
}

<h1>Review Requests</h1>

<p>Requests currently in progress and ready for manager review.</p>

<table class="table">
    <thead>
        <tr>
            <th>Ticket ID</th>
            <th>Title of Request</th>
            <th>Created By</th>
            <th>Assigned To</th>
            <th>Priority</th>
            <th>Status</th>
            <th>Created</th>
            <th>Review</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Requests.Any())
        {
            @foreach (var item in Model.Requests)
            {
                var createdByName = item.createdBy != null
                ? $"{item.createdBy.fName} {item.createdBy.lName}".Trim()
                : "Unknown";

                var assignedToName = item.assignedTo != null
                ? $"{item.assignedTo.fName} {item.assignedTo.lName}".Trim()
                : "Unassigned";

                <tr>
                    <td>@item.requestID</td>
                    <td>@item.title</td>
                    <td>@createdByName</td>
                    <td>@assignedToName</td>
                    <td>@item.priority</td>
                    <td>@item.status?.statusName</td>
                    <td>@item.createdAt.ToString("MM/dd/yyyy")</td>
                    <td>
                        <form method="post" asp-page-handler="Close" style="display:inline;">
                            <input type="hidden" name="RequestId" value="@item.requestID" />
                            <button type="submit" class="btn btn-sm btn-primary">Mark as Closed</button>
                        </form>

                        <a asp-page="/RequestPages/Details" asp-route-id="@item.requestID" class="btn btn-sm btn-secondary">
                            View
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="8" class="text-center">No in-progress requests found.</td>
            </tr>
        }
    </tbody>
</table>